<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UserLogin</title>
    <!-- <link rel="stylesheet" href="/public/style.css"> -->
    <style>
        /* General Styles */
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: 'Poppins', sans-serif;
    }
    
    body {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        background: grey;
        position: relative;
    }
    
    .navbar {
        width: 200px;
        height: 100vh;
        background:black;
        padding: 20px;
        position: fixed;
        left: 0;
        top: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.6);
    }
    
    .logo {
        width: 100px;
        margin-bottom: 40px;
    }
    
    .navbar ul {
        list-style: none;
        width: 100%;
    }
    
    .navbar ul li {
        width: 100%;
        margin: 20px 0;
    }
    
    .navbar ul li a {
        text-decoration: none;
        color: white;
        font-size: 18px;
        padding: 10px 10px;
        display: block;
        transition: background 0.3s ease, box-shadow 0.3s ease;
        text-align: center;
        border-radius: 10px;
        border: 2px solid white
    }
    
    .navbar ul li a:hover {
        background: white;
        color: black;
        box-shadow: 0 0 5px #00bfff, 0 0 25px #00bfff, 0 0 50px #00bfff, 0 0 100px #00bfff;
    }
    
    /* Login Container */
    .login-container {
        position: relative;
        color: white;
        width: 80%;
        max-width: 350px;
        height: auto; /* Dynamic height based on content */
        padding: 40px;
        /* background: #1c1c1c; */
        /* background-image: conic-gradient(red, yellow, green, blue, black); */
        background-image: linear-gradient(to right, rgba(255,0,0,0), rgba(255,0,0,1));
        box-shadow: 0 15px 25px rgba(0, 0, 0, 0.5);
        border-radius: 10px;
        margin-left: 200px; /* Adjust for the navbar */
        margin-top: 50px; /* Center it vertically */
        border: 2px solid white;
    }
    
    .login-container h2 {
        color: #fff;
        text-align: center;
        margin-bottom: 30px;
    }
    
    /* Input Box */
    .login-container .inputBox {
        position: relative;
        margin-bottom: 20px; /* Consistent spacing */
    }
    
    .login-container .inputBox input,
    .login-container .inputBox select {
        width: 100%;
        padding: 10px;
        background: transparent;
        border: none;
        outline: none;
        color: #fff;
        font-size: 18px;
        letter-spacing: 1px;
        border-bottom: 2px solid #fff;
        transition: 0.3s;
    }
    
    .login-container .inputBox span {
        position: absolute;
        left: 0;
        padding: 10px;
        font-size: 18px;
        color: white; /* Light blue color */
        pointer-events: none;
        transition: 0.5s;
    }
    
    .login-container .inputBox input:focus ~ span,
    .login-container .inputBox input:valid ~ span,
    .login-container .inputBox select:focus ~ span {
        transform: translateY(-20px);
        font-size: 14px;
        color: white;
    }
    
    .login-container .inputBox i {
        position: absolute;
        left: 0;
        bottom: 0;
        width: 100%;
        height: 2px;
        background: white;
        border-radius: 4px;
        transition: 0.5s;
        transform: scaleX(0);
        transform-origin: right;
    }
    
    .login-container .inputBox input:focus ~ i,
    .login-container .inputBox input:valid ~ i {
        transform: scaleX(1);
        transform-origin: left;
    }
    
    /* Button */
    button {
        background: transparent;
        border: 2px solid #00bfff;
        width: 100%;
        padding: 10px 0;
        color: white;
        font-size: 18px;
        cursor: pointer;
        border-radius: 25px;
        transition: 0.3s;
        box-shadow: none; /* No glow by default */
    }
    
    button:hover {
        background: #00bfff;
        color: #fff;
        box-shadow: 0 0 10px #00bfff, 0 0 50px #00bfff, 0 0 100px #00bfff, 0 0 200px #00bfff;
    }
    
    /* Sign-up Link */
    .signup-link {
        text-align: center;
        margin-top: 20px;
    }
    
    .signup-link a {
        color: #fff;
        text-decoration: none;
        font-size: 14px;
        transition: color 0.3s;
    }
    
    .signup-link a:hover {
        color: #00bfff;
    }
    
       </style>
</head>

<body>
    <nav>
        <div class="navbar">
            <img src="your-logo.png" alt="Logo" class="logo">
            <ul>
                <li><a href="/admin">Admin</a></li>
                <li><a href="/loginDone">Login</a></li>
                <li><a href="/sign/up">Signup</a></li>
            </ul>
        </div>
    </nav>

    
   

